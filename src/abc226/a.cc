#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)

#ifndef _debug
int main() {
    double x;
    cin >> x;
    int res = round(x);
    cout << res << endl;
    return 0;
}
#endif