#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

ll solve(int n, ll c, vector<ll> x, vector<ll> v);
/*
int main() {
  int n;
  cin >> n;
  ll c;
  cin >> c;
  vector<ll> x(n), v(n);

  for(int i=0;i<n;i++) {
    cin >> x[i] >> v[i];
  }
  cout << solve(n, c, x, v);
}
*/

ll solve(int n, ll c, vector<ll> x, vector<ll> v) {
  return -1;
}
