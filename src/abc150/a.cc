#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

string solve(int k, int x) { return (500 * k >= x ? "Yes" : "No"); }
/*
int main() {
    int k, x;
    cin >> k >> x;
    cout << solve(k, x);
}
*/