#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

#ifdef ONLINE_JUDGE
int main() {
    int a, b, c;
    cin >> a >> b >> c;

    cout << (7 - a) + (7 - b) + (7 - c) << endl;
    return 0;
}
#endif