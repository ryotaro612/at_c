#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
static const ll MOD = 1000000007ll;
#ifndef _LOCAL
int main() {
    ll n, k;
    cin >> n >> k;
    vector<ll> a(n);
    for(int i = 0; i < n; i++)
        cin >> a[i];
    
    
    return 0;
}
#endif