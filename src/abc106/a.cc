#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

#ifdef ONLINE_JUDGE
int main() {
  int a, b;
  cin >> a >> b;
  cout << a * b + 1 - a - b << endl;
  return 0;
}
#endif
