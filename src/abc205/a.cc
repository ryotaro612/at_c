#include <bits/stdc++.h>
using namespace std;
typedef long double ld;

#ifdef ONLINE_JUDGE
int main() {
    ld a, b;
    cin >> a >> b;

    cout << fixed << setprecision(12) << a * b / (ld)100 << endl;
    return 0;
}
#endif