#include "abc242/a.cc"
#include <gtest/gtest.h>

TEST(abc242a, 1) {
  // cout << solve(30, 500, 20, 103) << endl;
  // cout << solve(50, 500, 100, 1) << endl;
  // cout << solve(1, 2, 1, 1000) << endl;

  // assert(false);
}
